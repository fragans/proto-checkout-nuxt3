export default defineAppConfig({
  ui: {
    colorMode: false,
    colors: {
      primary: 'blue'
    },
    input: {
      slots: {
        root: 'w-full',
      }
    },
    inputMenu: {
      slots: {
        root: 'w-full',
      }
    },
    formField: {
      slots: {
        root: 'w-full',
      }
    }
  }
})